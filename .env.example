# ─── Database ──────────────────────────────────────────────────────────────────
# SwapHaven has its own database — do NOT reuse the barter-stack DB.
DATABASE_URL=postgresql://user:password@localhost:5432/swaphaven

# ─── JWT ───────────────────────────────────────────────────────────────────────
# Generate secure secrets: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# ─── Server ────────────────────────────────────────────────────────────────────
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Set true on Railway (and other reverse proxies)
TRUST_PROXY=false

# Swagger at /api-docs (defaults to on in development, off in production)
ENABLE_API_DOCS=true

# Public URL for OpenAPI docs server dropdown (optional; defaults to request host)
# PUBLIC_API_URL=https://api.swaphaven.io

# Run migrations on Docker container start (local docker compose only; Railway uses railway.toml)
RUN_MIGRATIONS=false

# ─── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Use * only for development.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080

# ─── Rate Limiting ─────────────────────────────────────────────────────────────
# Max requests per window per IP for auth routes
AUTH_RATE_LIMIT_MAX=20
# Max requests per window per IP for general API routes
API_RATE_LIMIT_MAX=300
# Window duration in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# ─── Google Sign-In (optional — POST /api/auth/social) ────────────────────────
# GOOGLE_CLIENT_ID is the Web OAuth client ID (primary audience for ID tokens issued via GIDServerClientID).
# Set GOOGLE_IOS_CLIENT_ID / GOOGLE_ANDROID_CLIENT_ID when the mobile app has no GIDServerClientID
# configured and tokens are addressed to the platform client ID instead.
# GOOGLE_CLIENT_ID=<your-web-client-id>.apps.googleusercontent.com
# GOOGLE_IOS_CLIENT_ID=<your-ios-client-id>.apps.googleusercontent.com
# GOOGLE_ANDROID_CLIENT_ID=<your-android-client-id>.apps.googleusercontent.com

# ─── S3 media (optional — POST /api/media/presign) ─────────────────────────────
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_MEDIA_BUCKET=swaphaven-media-dev
# S3_MEDIA_PREFIX=listings
# S3_PRESIGN_EXPIRES_SEC=300
# CDN_BASE_URL=https://d1234.cloudfront.net
